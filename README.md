# Sitio público de CONTAFISCAL

Landing page servida con GitHub Pages. **Solo la página** — el producto vive
en el repositorio privado `contafiscal`.

Para actualizarla: edita `sitio/index.html` en el repo privado y copia el
archivo aquí (`index.html`), luego commit + push.
